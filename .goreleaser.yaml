version: 2

project_name: mobile-release

before:
  hooks:
    - go mod tidy

builds:
  - id: mobile-release
    main: ./cmd/mobile-release
    binary: mobile-release
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: default
    builds:
      - mobile-release
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - README.md
      - LICENSE*

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  disable: true

brews:
  - name: mobile-release
    repository:
      owner: MarlonJD
      name: homebrew-tap
    directory: Formula
    homepage: https://github.com/MarlonJD/mobile-release-tools
    description: Local-first mobile release CLI for iOS and Android
    license: GPL-3.0-or-later
    install: |
      bin.install "mobile-release"
    test: |
      system "#{bin}/mobile-release", "--help"

scoops:
  - name: mobile-release
    repository:
      owner: MarlonJD
      name: scoop-bucket
    homepage: https://github.com/MarlonJD/mobile-release-tools
    description: Local-first mobile release CLI for iOS and Android
    license: GPL-3.0-or-later

nfpms:
  - id: packages
    package_name: mobile-release
    vendor: MarlonJD
    homepage: https://github.com/MarlonJD/mobile-release-tools
    maintainer: marlonjd <burak.karahan@mail.ru>
    description: Local-first mobile release CLI for iOS and Android
    license: GPL-3.0-or-later
    formats:
      - deb
      - rpm
    bindir: /usr/bin
